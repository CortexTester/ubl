package cbx.ubl


enum class CurrencyCode {
    USD, CAD
}

enum class EnergyCostCode{
    AFE, CC
}

enum class ReferenceSchemeIdentifier{
    CbxQuotationNumber,
    CbxQuotationId,
}

enum class PartyIdSchemeIdentifier{
    CBX, DUNS, SelfAssigned, PartnerAssigned
}
